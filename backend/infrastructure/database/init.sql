-- This file is retained for reference only.
-- The project now uses Firebase Firestore instead of PostgreSQL.
-- See /firestore.rules and /firestore.indexes.json at the repository root
-- for the equivalent Firestore security rules and composite indexes.

