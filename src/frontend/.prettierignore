# Generated by `next dev` / `next build` and headed "should not be edited".
# Prettier rewrote its quotes, Next rewrote them back, and the file showed up
# dirty after every build.
next-env.d.ts

# Build and tooling output.
.next/
coverage/
playwright-report/
test-results/
